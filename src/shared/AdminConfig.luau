return {
	-- The game's owner (game.CreatorId) is always an admin, with zero setup needed. List any
	-- extra usernames here if a collaborator should also get admin commands -- resolved to
	-- UserIds once at server start.
	ExtraAdmins = {},

	CommandPrefix = ":",
}
