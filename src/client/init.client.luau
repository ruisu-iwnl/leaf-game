-- local BlowerConeIndicator = require(script.BlowerConeIndicator)
local AdminCommandBar = require(script.AdminCommandBar)
local AFKUI = require(script.AFKUI)
local AssetPreload = require(script.AssetPreload)
local BadgeNotificationUI = require(script.BadgeNotificationUI)
local CameraSetup = require(script.CameraSetup)
local CoinsUI = require(script.CoinsUI)
local CursorLock = require(script.CursorLock)
local HallOfFameUI = require(script.HallOfFameUI)
local HotbarUI = require(script.HotbarUI)
local InventoryUI = require(script.InventoryUI)
local ItemPreviewUI = require(script.ItemPreviewUI)
local LeafCollector = require(script.LeafCollector)
local LeafRenderer = require(script.LeafRenderer)
local MapProgressUI = require(script.MapProgressUI)
local MobileControls = require(script.MobileControls)
local NPCDialogues = require(script.NPCDialogues)
local NPCDialogueUI = require(script.NPCDialogueUI)
local OverheatUI = require(script.OverheatUI)
local PlayerListUI = require(script.PlayerListUI)
local QuestLogUI = require(script.QuestLogUI)
local QuestNotificationUI = require(script.QuestNotificationUI)
local QuestPathUI = require(script.QuestPathUI)
-- local RakeConeIndicator = require(script.RakeConeIndicator)
local RakeCooldownUI = require(script.RakeCooldownUI)
local RoundResultsUI = require(script.RoundResultsUI)
local RoundUI = require(script.RoundUI)
local SackPickupUI = require(script.SackPickupUI)
local SackUI = require(script.SackUI)
local SettingsUI = require(script.SettingsUI)
local StorePanelUI = require(script.StorePanelUI)
local ToastNotificationUI = require(script.ToastNotificationUI)
local TutorialPanelUI = require(script.TutorialPanelUI)
local VIPColorPickerUI = require(script.VIPColorPickerUI)
local VIPEffects = require(script.VIPEffects)

-- BlowerConeIndicator.Start() -- cone highlight disabled per request; re-enable by uncommenting
AdminCommandBar.Start()
AFKUI.Start()
AssetPreload.Start()
-- ToastNotificationUI.Show only exists AFTER ToastNotificationUI.Start() runs (it's a closure
-- defined inside Start(), not a static top-level function -- see that file's own Start()). Must
-- come before every thin wrapper on top of it (Badge/Quest notification UIs) -- this used to run
-- last (after all of them), which meant a toast firing before this line executed crashed with
-- "attempt to call a nil value" on ToastNotificationUI.Show.
ToastNotificationUI.Start()
BadgeNotificationUI.Start()
CameraSetup.Start()
CoinsUI.Start()
CursorLock.Start()
HallOfFameUI.Start()
HotbarUI.Start()
InventoryUI.Start()
ItemPreviewUI.Start()
LeafCollector.Start()
LeafRenderer.Start()
MapProgressUI.Start()
MobileControls.Start()
NPCDialogueUI.Start()
OverheatUI.Start()
PlayerListUI.Start()
QuestLogUI.Start()
QuestNotificationUI.Start()
QuestPathUI.Start()
-- RakeConeIndicator.Start() -- cone highlight disabled per request; re-enable by uncommenting
RakeCooldownUI.Start()
RoundResultsUI.Start()
RoundUI.Start()
SackPickupUI.Start()
SackUI.Start()
VIPColorPickerUI.Start()
SettingsUI.Start()
StorePanelUI.Start()
TutorialPanelUI.Start()
NPCDialogues.Start()
VIPEffects.Start()
